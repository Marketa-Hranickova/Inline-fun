specVersion: "v1beta"
serviceId: "inlinefun"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "inlinefun-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
